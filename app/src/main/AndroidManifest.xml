<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.holler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Holler">
        <activity android:name=".Create_feed"></activity>
        <activity android:name=".Feed" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".NewProjectActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PreferencePickerActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"/>
    </application>

</manifest>